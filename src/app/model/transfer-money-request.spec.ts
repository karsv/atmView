import { TransferMoneyRequest } from './transfer-money-request';

describe('TransferMoneyRequest', () => {
  it('should create an instance', () => {
    expect(new TransferMoneyRequest()).toBeTruthy();
  });
});
