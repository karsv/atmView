import { DepositMoneyRequest } from './deposit-money-request';

describe('DepositMoneyRequest', () => {
  it('should create an instance', () => {
    expect(new DepositMoneyRequest()).toBeTruthy();
  });
});
